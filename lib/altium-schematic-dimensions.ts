/**
 * A larger coordinate scale keeps fixed-size native Altium primitives, such as
 * power ports and one-unit strokes, in proportion with circuit-to-svg output.
 */
export const ALTIUM_UNITS_PER_CIRCUIT_UNIT = 40
